<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin">

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Pairing devices"
        android:id="@+id/buttonPairing"
        android:onClick="associateF"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:text="Select your destination device:"
        android:id="@+id/textViewLabel"
        android:layout_gravity="center_horizontal" />

    <Spinner
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:padding="7dp"
        android:id="@+id/spinnerDevices"
        android:spinnerMode="dropdown"
        android:background="#4D94FF"/>

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Connect"
        android:layout_marginTop="16dp"
        android:id="@+id/buttonConnect"
        android:layout_gravity="center_horizontal"
        android:onClick="serviceF"/>

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Disconnect"
        android:layout_marginTop="16dp"
        android:id="@+id/buttonDisconnect"
        android:layout_gravity="center_horizontal"
        android:onClick="disconnectF"/>

</LinearLayout>